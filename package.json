{
  "name": "gatsby-source-alloy-multi-api",
  "version": "1.0.0",
  "description": "Copy of gatsby-source-multi-api",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "build": "babel src --out-dir . --ignore __tests__",
    "prepare": "cross-env NODE_ENV=production npm run build"
  },
  "author": "Francis Menguito",
  "license": "MIT",
  "dependencies": {
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-preset-minify": "^0.5.0",
    "cross-env": "^5.2.0",
    "prettier": "^2.0.5"
  }
}
